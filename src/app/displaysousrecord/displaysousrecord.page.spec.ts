import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DisplaysousrecordPage } from './displaysousrecord.page';

describe('DisplaysousrecordPage', () => {
  let component: DisplaysousrecordPage;
  let fixture: ComponentFixture<DisplaysousrecordPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DisplaysousrecordPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
